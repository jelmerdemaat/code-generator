{
  "title": "Code generator",
  "name": "code-generator",
  "author": "Jelmer de Maat",
  "private": true,
  "version": "0.0.4",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.2",
    "nodemon": "^1.11.0",
    "normalize.css": "^5.0.0",
    "twig": "^1.10.4"
  },
  "description": "To generate codes",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon --inspect server.js",
    "start": "nodemon server.js",
    "prettier": "prettier *.js --write --single-quote --tab-width=4 & prettier api/*.js --write --single-quote --tab-width=4",
    "test": "ava"
  },
  "devDependencies": {
    "ava": "^0.18.2",
    "prettier": "^0.22.0"
  }
}
